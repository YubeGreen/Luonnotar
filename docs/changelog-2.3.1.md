# 努昂诺塔 2.3.1

## 重启提醒可靠性热修复

- `LOCKED_BOOT_COMPLETED` 阶段立即发布特权引擎重启提醒，不再等待用户解锁。
- 将待提醒状态保存到设备保护存储；`BOOT_COMPLETED`、`USER_UNLOCKED`、包更新和主进程启动都会补查。
- 只有当前代 shell UID 2000 完成有效握手，或用户明确关闭内置引擎后，才清除待提醒状态。
- 重启提醒改为持续通知，并移除单条通知的分组，避免 MIUI/OriginOS 自动分组后只留下空摘要。
- 迁移旧版已分组的 1111 通知时先移除旧记录，再以未分组形式重新发布。
